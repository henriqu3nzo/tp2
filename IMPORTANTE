O arquivo contento os dados (artigo.csv) deve ser inserido na pasta com os códigos fontes. Por motivos de facilidade no envio do trabalho, o arquivo não está dentro da pasta.
O arquivo "makefile" contém os códigos necessários para o funcionamento do trabalho, e exemplos no caso de algum problema.